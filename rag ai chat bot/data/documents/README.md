# Add agricultural knowledge documents here

Place trusted Indian agriculture PDFs, TXT files, Markdown files, or DOCX files in this directory, then run `python -m app.ingestion.ingest` from `backend/`.
